# book-96《全国临床检验操作规程》数字化与接入验收（2026-09-19 深夜）

## 范围

承接「互链、病种库、患者端移除与院内字段隐藏验收（2026-09-19）」的遗留项：book-96 的 OCR 数字化与软件接入。源书 1089 页纯扫描、无文本层。

## 转换过程与故障处置

- iCloud 逐出空壳：`brctl download` 静默无效，改用强制读取（`cat` 整文件）恢复落地，6 个分片 SHA-256 与 2026-09-15 拆分清单逐一一致。
- MinerU 桌面端云上传故障：chunk_01/02 三次「failed to read file」/卡死（文件本身完好、/tmp 本地副本同样失败）→ 判定为云端通道问题；用户从 mineru.net/apiManage 取得 API token 后改走官方 API（`file-urls/batch` → PUT OSS → 轮询 → 下载 zip）。注意：apiManage 的 Access Key/Secret Key 不能当 Bearer token 用（A0202），真正可用的是单独生成的 sk- 开头 token。
- chunk_02 连 API 的 vlm 模型也报 `parsing failed`，换 `pipeline` 模型通过——同一文件不同后端结果不同，属模型侧缺陷而非文件损坏。
- 分片明细与通道见书内 [验收报告](~/Documents/资料库/检验科知识库/96-全国临床检验操作规程-人卫2015-第4版/验收报告.md)。

## 数字化产物（书目录内）

- `全国临床检验操作规程_完整版.md`：4.55MB，页标 1072/1089（缺 3 个已核对空白页 + 14 个纯图/公式页，其页标共享下一内容页）。
- `分章/`：59 章（6 篇）+ 060 名词索引；前置页在 `分章/_非内容/`；3 章 OCR 未捕获章号（第二篇九、第三篇二、第五篇一）以原书目录注入章题。
- `OCR修正记录.md`：7 类处理与核对证据；`疑似截断缩写.md` 75 条告警初筛全部为误报（CV=变异系数等）；`待核对段落.md` 970 处为后续长期人工工作。
- `md分片/`：MinerU 原始产物与构建脚本（merge-book96.py、split-book96.py、unwrap_mineru_latex.py）归档。

## 软件接入

- `import-textbooks.ts` BOOKS 增加 book-96（标题 全国临床检验操作规程，短名 操作规程，2015 年第 4 版）。`pnpm books:import`：60 章、48,021 行、18,619 条检索条目，逐行覆盖核验通过、0 降级表格。
- `extract-book-items.ts` 同步纳入：166 条规程条目。互链总量由 39 项/40 链升至 **59 项/70 链**（book-96 贡献 26 链，如 血红蛋白测定、凝血酶原时间）。
- 全库搜索教材逐行索引由 20,848 条升至 39,467 条。

## 验证

- `pnpm check`：type-check + eslint + 118 单测 + 五路内容对账（content/manual/books 224 文件/books-items 7 书/diseases）+ 生产构建，全绿。
- `pnpm exec playwright test`：112/112（Chromium + WebKit）通过，含书架（7 本）、book-96 章节阅读、跳页、书内检索、资料库 book-96 页内阅读。

## 遗留

- OCR 未逐字校对：数值、参考区间、剂量以原书为准；本书 2015 年第 4 版须结合现行规范。
- 桌面端 MinerU 进程已在任务完成后退出（pkill）；API token 存于 `~/.config/mineru-token.env`（600 权限）。
