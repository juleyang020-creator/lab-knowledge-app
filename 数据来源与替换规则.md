# 试验版数据来源与替换规则

## 当前范围

初始试验的资料目录为 `/Users/juleyang/Documents/资料库/检验科知识库`。本次用户另外指定《2026标本采集手册(1).md》并明确要求全文入软件：其完整只读快照保存于 `public/content/xwh-manual-2026.md`，没有修改上传原件，也没有公开上传。后续用户明确要求整个资料库先接入本地软件，现已将盘点出的5份主PDF按原字节接入 `public/library/`，不修改源资料，且不授权公开上传或部署。

初始试验抽取《临床生物化学检验技术》第2版（徐克前、王传新主编，人民卫生出版社，2025年）的少量常规检查概念。其9条短引文和PDF物理页记录继续保留：葡萄糖162、HbA1c168、ALT269、AST264、肌酐290、尿素291、血脂186、CRP130、TSH230。手册来源则使用上传MD的行号与原书页码，不能标成已核验PDF。

## 资料库原文件与阅读边界

5份主件、2309页，以 `public/library/index.json` 和 `docs/审核/library-inventory.json` 对账。原PDF本地副本逐份SHA-256一致；12份派生PDF分片不重复计入。`scripts/import-library.py` 提供导入与 `--check` 检查，本机执行环境为 Python 3.13 + PyMuPDF。

软件提供原PDF按页阅读和书名/目录检索，未将所有教材逐项整理为检验词条，也未完成所有教材的全文文字索引；扫描版《全国临床检验操作规程》尚未做OCR。原PDF阅读保留图片与表格，不能把原件存在称为完成文字提取或医学审核。手册MD行号与原书页码仍按原定位使用，不能因接入PDF就声称已逐条核验其物理页映射。

临床词条按“临床意义→参考范围与解释→标本与其他”排列；未收录的范围/解释不补造。科室标签沿用原手册检验分组，不当成本院业务归属或患者应挂号的科室。文件证据以可点击的来源/原文链接呈现；短引文仍保存在数据与链接提示中，不再占用正文备注框。

## 手册全文和分组记录

- 源文件身份：首都医科大学宣武医院检验科，2026版，XWH-JY-CJ；`sourceId=xwh-manual-2026`。
- 原表518行，其中1行完全空白。生成517条有名称记录；空行保留在全文，不能虚构名称。原有11条通用教学内容不覆盖、不按同名自动合并。
- 记录ID按**来源版本＋原表锚点＋原表行序**生成；空行不重排行序。编号只用于软件定位，不是医院开单代码。
- 所有原字段保留为 `manual.fields`，空字符串、“-”、“无”、“同上”不互换；安全渲染上下标及换行，不把指数并为普通数字。
- “同上”保留原文，附同一张表同列向上找到的非空记录及值；不跨表解析，遇空字段中断继承，不据此做医学推断。
- `manual.notes`完整保留各表后的说明；组合内容与地点用原始列名，外送表的“送检要求”不误当普通“送检时间”。
- 全部章节、表格和注释由完整原文快照承载。标题分节可重新拼接为完全相同的源文件字节；查看时校验SHA-256，渲染不执行任意HTML。
- 业务要求标为宣武医院手册照录，不冒充其他医院制度。两端均可访问，但患者入口标注“专业手册原文，不是患者自行操作指南”，不生成患者自行操作、停药或个人诊断建议。
- 医学状态保持未校订/未审核；疑似源文错误在验收记录中标注待回源，不在导入程序中悄悄改值。

## 两类来源，逐段标记

- `document`：来自实际读取的文件，必须记录来源编号、PDF物理页或可定位位置、核对用短引文。界面以文献/原文超链接呈现，保留可访问的“文件依据”标记和页码。
- `model`：由模型补充，必须记录生成日期，界面始终标为“模型补充 · 待核实”，不自动伪装成文件内容。
- 本轮所有医学说明的审核状态均为 `unreviewed`，不能显示为“已通过医学审核”。

## 不填造的本院信息

本院开单名称、代码、标本具体要求、报告时效、采样地点没有本院文件依据时保持缺项，并在页面显示“本院信息待补充”。通用检验名称和教材用途不能冒充本院已开展项目。

## 后续替换方式

1. 保留同一个项目编号和同一个说明段落编号。
2. 在资料目录中读取实际文件，定位支持该段内容的页码或章节。
3. 核对并必要时改写正文；将这一段的来源从 `model` 改为 `document`，填写出处和短引文。
4. 运行内容校验与页面测试；更新来源覆盖情况。不能只改来源标签、不核对正文。
5. 完成全部替换后启用“只允许文件依据”检查模式；只要仍有模型段落，检查就应失败。

文件覆盖率只统计医学说明段落的来源，不代表医学准确率，也不掩盖本院业务信息的缺项。页面文案、菜单和程序代码不计入医学来源覆盖率。

## 试验边界

静态模型补充不是实时问诊或AI聊天服务。不收集病历、个人检验结果或身份信息，不给出个体诊断、治疗或自动开单。高风险病原体操作、培养工程、毒素及传播等可被滥用的细节不纳入试验内容。

试验程序已采用TypeScript/Vue/Vite；所有应用文件、数据摘录和核验报告均保存在项目内。原始资料库保持只读。

`pnpm content:check` 检查数据结构、编号、来源引用、页码范围与模式；`pnpm manual:check` 重新解析原文并对全部项目、字段、说明及源文件身份逐项对账，两者均纳入 `pnpm check`。校验不自动判断医学语义是否正确。`pnpm content:check --require-documents` 在原有53段模型补充尚未替换时继续按预期拒绝通过。
