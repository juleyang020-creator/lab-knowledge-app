# 打开检验知识库

这是在电脑浏览器里运行的**本地试验版**，不是安装好的原生 App，也还没有上线公众号。医学内容仍未完成审核。

## 平时怎么打开

在本项目文件夹里双击对应入口：

- **Mac：`打开检验知识库-macOS.command`**（本机已经具备运行环境）。
- **Windows：`打开检验知识库-Windows.cmd`**。

入口会启动本地服务，并自动打开浏览器中的 **http://127.0.0.1:4317/**。不要直接打开 `index.html`，也不要只把入口文件单独拷走。

**保留最初打开的终端/命令提示符窗口。** 退出时在这个窗口按 **Ctrl+C**；也可以关闭这个最初的窗口。只关闭浏览器标签不会停止服务。重复双击只会打开已有页面，关闭重复产生的窗口不影响主服务。

如果浏览器没自动打开，手动访问上面的地址。服务只监听本机，不向局域网或公网开放；入口不会上传项目资料。

## 第一次在另一台电脑上运行

1. 把**完整项目文件夹解压到本机磁盘**。需要保留 `scripts`、`src`、`public`、`package.json`、`pnpm-lock.yaml`、Vite 配置等源码文件。
2. 安装 **Node.js 22.x，且不低于 22.23.1**。目前没有验证 Node 24/其他主版本，不要直接选择网站默认的其他版本。
   - [Node.js 22.x 官方下载目录](https://nodejs.org/download/release/latest-v22.x/)
   - Windows 选择与电脑架构对应的 `.msi` 安装包，普通 Intel/AMD 电脑通常选 `x64.msi`；安装时保留 **Add to PATH** 和 npm。
   - Mac 可选择 `.pkg` 安装包。
3. 安装完成后重新双击对应入口。第一次需要联网，入口会自动安装本项目的锁定依赖；请等到浏览器打开。
4. 不必提前全局安装 pnpm。已安装版本不匹配或没有 pnpm 时，入口会借助 Node 自带的 npm 临时运行项目要求的 pnpm，不改全局版本。

**从 Mac 拷到 Windows 时不要复制 `node_modules`。** 这个目录内含平台专用依赖，应在目标电脑上由入口重新安装。`dist` 也不是启动入口的必需文件；入口运行的是当前源码，避免打开过期构建。不要从压缩包内直接双击运行，也不建议放在网络共享盘。

## 打不开时

- **提示找不到 Node 或版本不支持**：按上面步骤安装受支持的 Node；关闭旧窗口后重新双击。错误窗口会保留提示。
- **安装失败**：检查网络与代理，保留报错内容；修好网络后重新双击。不会自动更改锁文件或升级依赖。
- **提示端口被占用**：可能有旧的 `pnpm dev` 窗口或另一个项目副本占用了 `4317`。在那个窗口停止服务再运行本入口；启动器不会擅自结束其他程序，也不会切换端口导致收藏看起来消失。
- **页面无法访问**：先确认主启动窗口还开着；代理软件不要代理 `127.0.0.1`/`localhost`。
- **Mac 提示来源或权限问题**：确认文件来自可信项目后，用系统提供的“右键→打开”确认；不要关闭系统安全保护。本机入口已经设置执行权限，从别处下载/解压后若权限丢失，需要重新赋予该文件执行权限。
- **Windows 弹出安全提醒**：先核对文件来自本项目，再按系统正常流程确认；不要关闭系统防护。请保留文件的 `.cmd` 后缀。

## 验证边界

macOS 入口在本机验证；Windows 入口已检查命令结构、编码和共用启动逻辑，但**尚未在 Windows 实机双击验收**。这两份文件是源码项目的便捷启动入口，不是免运行环境的 `.app`/`.exe` 安装包。
