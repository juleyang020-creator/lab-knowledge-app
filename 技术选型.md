# 技术选型：TypeScript + Vue 3 + Vite

> **状态：已用于试验版。** 已建立 TypeScript / Vue / Vite 应用、内容数据层与测试；正式部署、专业端准入和医院系统对接仍未实施。运行方式见 [README](./README.md)。

## 一、主语言建议

**试验版主编程语言采用 TypeScript。**

TypeScript 在 JavaScript 基础上增加类型表达，代码可转换为 JavaScript 在浏览器等环境运行。[2] 因此本项目可以先做微信内打开的网页，而不是分别开发 iPhone 和 Android 安装包。

这里的选择不代表所有文件都用同一种语言：页面还会使用 HTML/CSS，内容数据使用 JSON/Markdown；这些分别承担页面结构、样式和资料存储。

| 层面 | 推荐选择 | 在本项目中负责什么 |
| --- | --- | --- |
| 主要编程语言 | **TypeScript** | 双入口切换、搜索、项目数据模型、收藏和访问规则接入 |
| 界面框架 | **Vue 3** | 专业人士与患者两种首页、项目卡片、详情页和交互组件 |
| 开发与构建工具 | **Vite** | 本地开发、生成用于网页部署的文件 |
| 页面结构与样式 | HTML + CSS | 手机排版、选项卡、表格、字体、图片和不同屏幕适配 |
| 结构化内容 | JSON | 项目编号、名称、别名、组合关系、开展状态、标本与服务字段 |
| 医学说明与项目文档 | 结构化 JSON + Markdown 文档 | 当前医学段落统一用 JSON 保存来源字段，Markdown 用于维护文档；后续再评估长篇正文格式 |
| 后续后台 | Node.js + TypeScript，届时评估 | 专业端访问要求、受控内容接口、在线维护和账户相关功能 |
| 辅助资料处理 | Python，按需使用 | 后续可能的 Excel/Word 整理、数据检查与格式转换；不是首版前台主语言 |

Vue 官方提供 TypeScript 支持；官方脚手架 `create-vue` 可以生成基于 Vite、支持 TypeScript 的项目。[3] 因此建议采用官方工具，而不是自行搭建一套构建系统。

## 二、为什么建议这样选

以下是结合本项目需求作出的技术判断，而不是宣称其他语言不能完成。

### 1. 适合当前交付形式

当前先做公众号内打开的手机网页，重点是搜索、分类、图文与两种阅读视图。优先采用网页技术，不先承担原生 App 的安装包、商店发布和多客户端维护工作。

### 2. 适合结构化检验项目资料

项目数据会涉及开单项目、报告指标、组合关系、标本要求、开展状态和内容版本。建议用明确的类型表达这些关系，帮助开发时识别字段拼错、遗漏和误用。

**类型检查只能减少程序层面的错误，不能证明医学内容正确，也不能替代导入数据校验和专业审核。**

### 3. 双入口可以复用同一份数据

以共同的项目编号关联：

- 公共基础资料；
- 专业扩展；
- 患者说明。

Vue 的两种视图使用同一份基础事实，各自组织内容重点，避免两个独立应用复制项目资料后逐渐不一致。

### 4. 后续加专业端查看要求时保留扩展边界

首版不建设登录、专业认证或审核流程。先将专业内容加载、专业搜索索引和附件组织独立，避免把全部专业内容硬编码进患者端公共脚本。

后续启用查看要求时，再通过服务端或受控内容服务检查访问条件。**TypeScript 和 Vue 本身不提供真实的访问控制；前端隐藏按钮也不算保护内容。** 后续阶段仍需要实施与验证相应后台能力。

## 三、为什么暂不选择其他路线

| 路线 | 本阶段的取舍 |
| --- | --- |
| 纯 JavaScript | 可以实现，但本项目预期会长期维护结构化资料和两端视图，优先采用类型约束更明确的 TypeScript |
| Python 作为主站技术 | 适合资料处理，也可以开发后端；但本轮主要工作是网页界面与交互，不为尚未需要的服务端增加另一套主技术栈 |
| React | 同样可行；当前没有必须使用 React 的约束，建议先统一采用 Vue，不同时维护两套框架 |
| Flutter / Swift / Kotlin | 面向其他客户端路线时再评估；当前目标不是独立安装包 |
| 先上大型后台与数据库 | 当前没有个人报告、登录或多人在线维护的已确认需求，不提前增加运维与数据迁移负担 |

## 四、内容和 GitHub 如何配合

### 第一阶段：先管理内容版本

- 当前已建立应用代码、结构化试验目录、专业和患者两套说明文件。
- 按用户要求，初期采用指定文件夹依据加显式标注的模型补充；医学审核状态保持未审核，不冒充本院已批准资料。
- 代码与经审查的内容均可通过 Git 记录修改历史、审核和回退。
- 每次上传只包含明确列出的文件，排除凭证、患者资料和未批准的内部文件。

### 应用上线另行处理

GitHub 仓库用于保存和同步源码与内容，不等于公众号已能打开应用。本轮不启用 GitHub Pages，不购买域名、不配置服务器或公众号。

应用前台公开与源码仓库公开是独立选择。仓库先采用私有可见性以避免意外公开资料；后续如要开源，再确认许可与全部历史内容。

### 专业端限制是后续独立阶段

具体查看要求、身份核验方式、数据存储与部署位置，届时再定。患者端继续公开；专业端限制必须覆盖内容本身、搜索、附件和直达链接。

首版已经公开的资料不能保证从读者副本中收回，未来准备保密的资料不应先放入当前开放版本。

## 五、试验版采用的实施顺序

1. 使用官方 `create-vue` 初始化 TypeScript 项目，并在实际初始化时核实依赖兼容性、固定版本与锁文件。
2. 建立“我是专业人士”“我是患者”双入口、两种首页和共同项目编号。
3. 定义公共基础字段、专业扩展、患者说明及开单组合与报告指标关系。
4. 完成搜索、详情、入口切换与本地收藏；首版专业端访问检查保持开放。
5. 加入类型检查、单元测试、生产构建与手机浏览器验收，再考虑部署。

Vue 官方说明，基于 Vite 的开发与打包流程默认负责转译，不执行类型检查；Vue 单文件组件应单独使用 `vue-tsc` 等工具进行检查。[3] 因此以后不能只凭“打包成功”就宣称类型检查已通过。

已使用官方脚手架作为配置基础并安装锁定依赖；项目提供 `pnpm dev`、`pnpm check`、`pnpm test:e2e` 和 `pnpm content:check`。具体命令与边界见 [README](./README.md)，真实验证结果见验收报告。

## 六、本轮决策状态

- **产品需求已确认：** 双入口；初期两端无查看限制；后续再限制专业人士入口。
- **已采用技术：** TypeScript + Vue 3 + Vite；JSON 保存试验内容与逐段来源，Markdown 保存项目文档。
- **已进入试验：** 双入口均开放，文件摘读和模型补充分开标注；未完成医学审核。
- **后续再选：** 后台框架、数据库、专业准入方式与生产部署服务。

## Sources

[2] https://www.typescriptlang.org — TypeScript: JavaScript With Syntax For Types
[3] https://vuejs.org/guide/typescript/overview.html — Using Vue with TypeScript | Vue.js
